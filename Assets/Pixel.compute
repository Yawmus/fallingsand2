// Each #kernel tells which function to compile; you can have many kernels
#pragma kernel Move


struct Item
{
	float3 pos;
};

RWStructuredBuffer<Item> dataBuffer;

[numthreads(8,8,1)]
void Move (uint3 id : SV_DispatchThreadID)
{
	// TODO: insert actual code here!
	dataBuffer[id.x].pos = dataBuffer[id.x].pos + float3(1 % 10, 0, 0);
}
